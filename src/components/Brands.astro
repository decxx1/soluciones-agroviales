---
import brands from '@/data/Brands.json'
---
<section class="container px-6 py-10 mx-auto">
    <div class="text-center">
        <h2 class="mb-4 max-w-xl mx-auto text-2xl md:text-3xl lg:text-4xl font-bold leading-none tracking-tight text-gray-900 dark:text-primary-300">
            Marcas
        </h2>
        <p class="mb-6 text-lg font-normal text-gray-500 lg:text-xl sm:px-16 xl:px-48 dark:text-gray-400">
            Trabajamos con maquinarias viales de todas las marcas
        </p>
    </div>
    <div class="mt-6 space-y-4 md:grid md:grid-cols-2 lg:grid-cols-5 md:gap-4 xl:gap-8 md:space-y-0 md:mb-8 md:mt-12">
        
        {brands.map((brand) => (
            <div class="flex justify-center items-center p-8 text-center bg-gray-50 rounded hover:bg-gray-100 dark:hover:bg-gray-600 dark:bg-gray-700">
                <img src={brand.src} alt={brand.alt} class="w-48" />
            </div>
        ))}
        
    </div>
</section>