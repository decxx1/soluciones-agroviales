---
import brands from '@/data/Brands.json'
---
<section class="container px-6 py-10 mx-auto">
    <div class="text-center">
        <h2 class="mb-4 max-w-xl mx-auto text-2xl md:text-3xl lg:text-4xl font-bold leading-none tracking-tight text-primary-300 dark:text-primary-300">
            Marcas
        </h2>
        <p class="mb-6 text-lg font-normal text-gray-300 lg:text-xl sm:px-16 xl:px-48 dark:text-gray-400">
            Trabajamos con maquinarias viales de todas las marcas
        </p>
    </div>
    
    <!-- Permanent -->
    <div class="max-md:hidden mt-6 space-y-4 md:grid md:grid-cols-3 lg:grid-cols-5 md:gap-4 xl:gap-8 md:space-y-0 md:mb-8 md:mt-12">
        {brands.map((brand) => (
            <div class="flex justify-center items-center p-4 text-center bg-gray-600 rounded hover:bg-gray-500 dark:hover:bg-gray-600 dark:bg-gray-700">
                <img loading="lazy" src={brand.src} alt={brand.alt} class="w-48" />
            </div>
        ))}
    </div>

    <!-- Carousel -->
    <div id="controls-carousel" class="relative w-full md:hidden" data-carousel="static">
        <!-- Carousel wrapper -->
        <div class="relative h-32 sm:h-40 overflow-hidden rounded">
            {brands.map((brand) => (
                <div class="hidden duration-700 ease-in-out p-4 text-center bg-gray-600 rounded hover:bg-gray-500 dark:hover:bg-gray-600 dark:bg-gray-700" data-carousel-item>
                    <img loading="lazy" src={brand.src} alt={brand.alt} class="absolute block w-72 -translate-x-1/2 -translate-y-1/2 top-1/2 left-1/2" />
                </div>
            ))}
        </div>
        
        <!-- Slider controls -->
        <button type="button" class="absolute top-0 start-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-prev>
            <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
                <svg class="w-4 h-4 text-white rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 1 1 5l4 4"/>
                </svg>
                <span class="sr-only">Previous</span>
            </span>
        </button>
        <button type="button" class="absolute top-0 end-0 z-30 flex items-center justify-center h-full px-4 cursor-pointer group focus:outline-none" data-carousel-next>
            <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-white/30 dark:bg-gray-800/30 group-hover:bg-white/50 dark:group-hover:bg-gray-800/60 group-focus:ring-4 group-focus:ring-white dark:group-focus:ring-gray-800/70 group-focus:outline-none">
                <svg class="w-4 h-4 text-white rtl:rotate-180" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
                    <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
                </svg>
                <span class="sr-only">Next</span>
            </span>
        </button>
    </div>

</section>
